# apiVersion: v1
# kind: Secret
# metadata:
#   name: wordpress-tls-secret
# type: kubernetes.io/tls
# data:
#   tls.crt: crt.pem
#   tls.key: key.pem
# 파일 내용이 길어서 시크릿 파일 안에 라인으로 적을 수 없다.
# 그리고 오히려 kubectl create secret tls 로 만드는 것이 훨씬 편하다.
